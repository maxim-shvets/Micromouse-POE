add todo items and bugs here as they are found
1. Fix race mode in alogithm
2. fix maze generation so that the maze cannot be solved simply by following a wall
3. write a full algorithm that runs based on micromouse regulations (go to goal square and return in order to plan, then after mapping maze, then race (see micromouse rules))
4. 